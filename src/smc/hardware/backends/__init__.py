"""Backends: capability implementations over a concrete hardware bus.

Only Micro-Manager (``mm``) exists in M1; stands Micro-Manager cannot reach
natively get unicore Python devices behind the same core in M4 (ADR-0002),
so this package is expected to stay small.
"""
